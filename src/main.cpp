#include <SDL.h>
#include "controls.h"
#include "player.h"

// Твоя глобальная логика
int main(int argc, char* argv[]) {
    // Инициализация SDL, создание окна
    // Игровой цикл (while running)
    // Обработка событий
    // Вызовы controls::update(), player::update()
    // Вызовы player::draw(), controls::draw()
    return 0;
}
